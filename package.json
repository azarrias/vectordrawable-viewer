{
  "name": "vectordrawable-viewer",
  "displayName": "VectorDrawable Viewer",
  "icon": "icon.png",
  "publisher": "collectiveofone",
  "description": "VS Code extension that lets you visualize VectorDrawable XML files. Ideal for quickly iterating edits of simple VectorDrawable assets.",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Visualization"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onCommand:vectordrawable.viewer"
  ],
  "contributes": {
    "configuration": {
      "properties": {
        "vectordrawableViewer.defaultWidth": {
          "type": "string",
          "default": "24",
          "description": "Default width when android:width is missing."
        },
        "vectordrawableViewer.defaultHeight": {
          "type": "string",
          "default": "24",
          "description": "Default height when android:height is missing."
        },
        "vectordrawableViewer.defaultAlpha": {
          "type": "string",
          "default": "1",
          "description": "Default alpha when android:alpha is missing."
        },
        "vectordrawableViewer.defaultTint": {
          "type": "string",
          "default": "",
          "description": "Default tint color when android:tint is missing."
        },
        "vectordrawableViewer.defaultTintMode": {
          "type": "string",
          "default": "src_in",
          "description": "Default tint mode when android:tintMode is missing."
        },
        "vectordrawableViewer.defaultFillColor": {
          "type": "string",
          "default": "#00000000",
          "description": "Default fill color when android:fillColor is missing."
        },
        "vectordrawableViewer.defaultFillAlpha": {
          "type": "string",
          "default": "1",
          "description": "Default fill alpha when android:fillAlpha is missing."
        },
        "vectordrawableViewer.defaultStrokeColor": {
          "type": "string",
          "default": "#00000000",
          "description": "Default stroke color when android:strokeColor is missing."
        },
        "vectordrawableViewer.defaultStrokeWidth": {
          "type": "string",
          "default": "0",
          "description": "Default stroke width when android:strokeWidth is missing."
        },
        "vectordrawableViewer.defaultStrokeAlpha": {
          "type": "string",
          "default": "1",
          "description": "Default stroke alpha when android:strokeAlpha is missing."
        },
        "vectordrawableViewer.defaultStrokeLineCap": {
          "type": "string",
          "default": "butt",
          "description": "Default stroke line cap when android:strokeLineCap is missing."
        },
        "vectordrawableViewer.defaultStrokeLineJoin": {
          "type": "string",
          "default": "miter",
          "description": "Default stroke line join when android:strokeLineJoin is missing."
        },
        "vectordrawableViewer.defaultStrokeMiterLimit": {
          "type": "string",
          "default": "4",
          "description": "Default stroke miter limit when android:strokeMiterLimit is missing."
        },
        "vectordrawableViewer.defaultTrimStart": {
          "type": "string",
          "default": "0",
          "description": "Default trim start when android:trimStart is missing."
        },
        "vectordrawableViewer.defaultTrimEnd": {
          "type": "string",
          "default": "1",
          "description": "Default trim end when android:trimEnd is missing."
        },
        "vectordrawableViewer.defaultTrimOffset": {
          "type": "string",
          "default": "0",
          "description": "Default trim offset when android:trimOffset is missing."
        },
        "vectordrawableViewer.defaultFillType": {
          "type": "string",
          "default": "nonZero",
          "description": "Default fill type when android:fillType is missing."
        },
        "vectordrawableViewer.defaultAutoMirrored": {
          "type": "boolean",
          "default": false,
          "description": "Default autoMirrored when android:autoMirrored is missing."
        }
      }
    },
    "commands": [
      {
        "command": "vectordrawable.viewer",
        "title": "Open VectorDrawable Viewer"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -p . --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/azarrias/vectordrawable-viewer.git"
  },
  "bugs": {
    "url": "https://github.com/azarrias/vectordrawable-viewer/issues"
  },
  "homepage": "https://github.com/azarrias/vectordrawable-viewer#readme",
  "license": "CC-BY-NC-SA-4.0",
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/vscode": "^1.60.0",
    "typescript": "^7.0.2"
  }
}
